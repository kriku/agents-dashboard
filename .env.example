# BFF API base URL (separate domain in production, empty for MSW mocks in dev)
# Production: https://api.monitoring.example.com
# Local BFF:  http://localhost:8080
VITE_API_BASE_URL=

# Enable MSW mocks in non-dev builds (dev mode enables mocks automatically)
# VITE_MOCK_API=true
