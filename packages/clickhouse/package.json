{
  "name": "@agent-monitor/clickhouse",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "migrate": "echo 'Init scripts applied via docker-entrypoint-initdb.d (see init/)'",
    "seed": "tsx seed/seed.ts",
    "generate": "tsx seed/generate.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-monitor/shared": "workspace:*",
    "@clickhouse/client": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.19.0",
    "typescript": "~5.6.0",
    "vitest": "^2.1.0"
  }
}
